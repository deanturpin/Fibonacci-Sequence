#!/bin/bash

# Build everything that needs building
make -j

# Every thing that runs starts with an underscore
for file in _*; do

	if [[ $(./$file) == 120 ]]; then
		echo -e $file\\tpass
	else
		echo -e $file\\tfail
	fi
done
